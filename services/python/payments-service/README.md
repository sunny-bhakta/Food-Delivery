# Payments Service 
FastAPI service orchestrating PSP integrations and webhooks. 
 
## Dev workflow 
1. python -m venv .venv && .venv\Scripts\activate 
2. pip install -r requirements.txt 
3. uvicorn app.main:app --reload --port 5002
